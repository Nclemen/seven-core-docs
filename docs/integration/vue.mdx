---
sidebar_label: 'vue integration'
sidebar_position: 2
hide_table_of_contents: true
title: vue integration
---

<h3>installing</h3>

<p>by running the code below the seven-core package will be installed.</p>

```jsx
npm install @nclemen/seven-core@0.0.31;
```

<h3>importing</h3>

```jsx title="index.html"
import Vue from 'vue';
import { applyPolyfills, defineCustomElements } from '@nclemen/seven-core/loader';

// Tell Vue to ignore all components defined in the svn-core
// package. The regex assumes all components names are prefixed
// 'svn'
Vue.config.ignoredElements = [/svn-\w*/];

// Bind the custom elements to the window object
applyPolyfills().then(() => {
    defineCustomElements();
});

new Vue({
    render: h => h(App)
}).$mount('#app');
```

<h3>Playground</h3>

<iframe src="https://codesandbox.io/embed/winter-glade-ypzzvd?fontsize=14&hidenavigation=1&theme=dark"
        style={{
            width:"100%", height:"800px", border:"0", "border-radius": "4px", overflow:"hidden",
        }}
        title="winter-glade-ypzzvd"
        allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
        sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"></iframe>

<a href=""></a>